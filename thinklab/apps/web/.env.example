# Leave unset for local dev (uses the Vite proxy to localhost:4000).
# Set this to your deployed API's URL when building for production, e.g.:
# VITE_API_BASE_URL=https://thinklab-api.onrender.com/api
VITE_API_BASE_URL=
